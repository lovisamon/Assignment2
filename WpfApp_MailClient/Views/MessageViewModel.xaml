<UserControl x:Class="WpfApp_MailClient.Views.MessageViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp_MailClient.Views"
             xmlns:models="clr-namespace:WpfApp_MailClient.Models"
             xmlns:controls="clr-namespace:WpfApp_MailClient.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="messageMenu" Grid.Column="0" Background="LightSteelBlue">
            <StackPanel>
                <TextBlock Text="Messages" FontSize="30" FontFamily="Century Gothic" TextAlignment="Left" Padding="20,40,0,40"/>
                <StackPanel x:Name="MessageList"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="userControlCurrentMessage" Grid.Column="1" Background="Transparent"/>

    </Grid>
    
</UserControl>
